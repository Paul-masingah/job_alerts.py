Install python-dotenv to load these automatically
At the top of job_alerts.py, load your .env
It will fetch jobs → filter → send test email → print results in console.

If it works locally, GitHub Actions will also work when scheduled. ✅
